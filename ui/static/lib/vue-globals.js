const {
  createApp,
  reactive,
  computed,
  onMounted,
  onBeforeUnmount,
  ref,
  watch,
} = window.Vue;

const {
  createRouter,
  createWebHashHistory,
  useRoute,
} = window.VueRouter;

export {
  createApp,
  reactive,
  computed,
  onMounted,
  onBeforeUnmount,
  ref,
  watch,
  createRouter,
  createWebHashHistory,
  useRoute,
};
